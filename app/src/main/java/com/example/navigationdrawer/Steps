First of all add the dependencies.

1). First make the drawer layout as the root layout of the activity_main.xml
2). Then add the layout of the activity and the navigation drawer.
3). Then add the nav header layout.
4). Then add the Menu layout.
5). Then we are will write code in MainActivity for hamburger button.